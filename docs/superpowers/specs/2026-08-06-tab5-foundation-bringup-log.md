# Foundation Phase — Bring-Up Log

Date completed: 2026-08-09

| # | Definition of Done item | Verified in | Result |
|---|---|---|---|
| 1 | Multi-target build produces both .bin files | Task 1 | PASS — both firmwares build clean on the current toolchain (Tab5 via a custom `quarky_tab5_p4` board on pioarduino; Cardputer-ADV via `m5stack-stamps3` on pioarduino). Verified via clean rebuilds throughout the session, most recently as part of Task 20's final commit. |
| 2 | LVGL boots, touch responsive, lv_keyboard works | Tasks 5, 6, 8 | PASS, with one caveat. Display (Task 5) and touch (Task 6) are both real-hardware confirmed: the shell UI renders correctly right-side-up on the physical panel, and touch registers real DOWN events with correct coordinates across the full screen (confirmed by tapping "Pair Satellite" and seeing the pairing flow actually fire). `lv_keyboard` itself (Task 8) passed its own task review with no findings, but was not separately re-exercised on real hardware as its own isolated check this session — its correctness rests on the general touch-responsiveness confirmation above plus the original code review, not a dedicated live keyboard-input test. Worth a quick dedicated check in Phase 2 if a feature actually depends on virtual-keyboard text entry. |
| 3 | esp-hosted WiFiRemote confirmed live | Task 9 | PASS — the ESP32-C6 co-processor is confirmed live on real hardware (90-second soak test, zero brownout resets, after the pin+power hotfix). Tab5's WiFi AP comes up and Cardputer-ADV associates to it; Task 20's real end-to-end ping over WiFi (50ms round-trip) is the strongest possible confirmation this path genuinely works, not just links. |
| 4 | SD + C6 SDIO bus-sharing resolved | Task 10 | PASS — confirmed via 4 independently cross-checked sources that SD (SDMMC slot 0) and the C6 (SDIO slot 1) sit on separate hardware slots, no shared bus. Real hardware confirmation: SD mount and write succeeded during the same 90-second soak test that confirmed C6 stability, with both subsystems active simultaneously. |
| 5 | PSK provisioning flow completed on real hardware | Task 12 | PASS, functionally — the pairing screen generates a PSK, persists it to NVS, and that exact key was used (read via serial, hand-provisioned into Cardputer-ADV) to authenticate a real HMAC-verified round trip in Task 20. The underlying flow works. **UX caveat, not a functional failure:** the pairing screen's QR code is unusable as designed (Cardputer-ADV has no camera to scan it) and the on-screen back button overlaps the QR code and is small enough to be hard to tap reliably. Left unfixed deliberately — out of scope for foundation hardware bring-up — and flagged in `docs/phases/phase-1-foundation.md` for any future Tab5 UI work. |
| 6 | WiFi socket + BLE GATT transports both verified | Tasks 11, 13, 15, 17, 20 | PASS — both transports complete a full, real hardware round trip in Task 20's final committed state: WiFi (`connected via WiFi (50ms)`) and BLE (`connected via BLE (105ms)`), same firmware, transport selection driven by which one is actually up. Getting here required two real hardware-only bug fixes beyond the original per-task work: a BLE receive-drain loop that was unreachable once connected (`C2LinkBle::poll()`), and a missing `BLE_GATT_CHR_F_WRITE_NO_RSP` flag on Tab5's Rx characteristic that caused the BLE server to silently drop every reply. Both fixed and verified; see `docs/phases/phase-1-foundation.md`. |
| 7 | Cardputer-ADV standalone operation unmodified | Task 16 | PASS — the local menu baseline runs with zero Tab5 connection (Task 16), and this held true throughout every isolated Cardputer-ADV hardware bring-up session before Task 20 ever connected it to Tab5 (including the PSRAM-crash hotfix verification, which ran entirely standalone). |
| 8 | End-to-end ping feature demonstrated live | Task 20 | PASS — real hardware, both devices simultaneously connected, both transports independently exercised, real telemetry replies received and logged on the Tab5 side (`telemetry received: uptime=Ns`). This is the strongest single piece of evidence that the whole foundation contract (FeatureRegistry, c2proto framing/HMAC, both `IC2Link` transports) actually works end-to-end, not just compiles. |
| 9 | Tab5 NFC/RFID2/RF433 HAL drivers detect hardware | Task 18 | PARTIAL. NFC: PASS — once the HY2.0 port's power-gating bug was found and fixed (same root cause class as the C6 fix), a real connected NFC unit was detected at I2C address 0x50; it turned out to be an ST25R3916, not the PN532 originally assumed (only the I2C ACK is confirmed, no protocol-level verification yet — flagged for Phase 3). RFID2: UNTESTED — no RFID2 unit was physically connected during HY2.0 bring-up. RF433: UNTESTED — the real GPIO pin assignment for this HY2.0 sub-peripheral was never determined from any available source, and the code deliberately refuses to touch an unverified pin (`-1` guarded TODO) rather than guess. |

## Summary

8 of 9 items are fully PASS on real hardware. Item 9 (peripheral HAL detection) is genuinely partial — one of three peripheral types (NFC) is hardware-confirmed, the other two (RFID2, RF433) need either a physical unit or a real pin-assignment source before Phase 3 work on them can start from verified ground truth rather than placeholders. Item 5's functional flow is fully proven but carries a real, disclosed UX debt (QR/back-button) that should not be carried forward uncritically into future Tab5 UI phases.

No item is a FAIL. Phase 1's foundation — multi-target build, LVGL UI shell with working touch, dual WiFi/BLE C2 transport, PSK-based pairing, and the FeatureModule/FeatureRegistry contract proven end-to-end via a real feature — is complete and hardware-verified.

See `docs/phases/phase-1-foundation.md` for the full narrative (architecture decisions, every real-hardware root cause found and fixed, and the complete list of known limitations/deferred work for Phase 2+ to build from).
